/**
 * @file
 * blog behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.blog = {
    attach: function (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
